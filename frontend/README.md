# Ayanm Flutter Frontend

This repository contains a minimal Flutter application that connects to the Ayanm backend.

## Setup

```bash
cd frontend
flutter pub get
flutter run
```

The app fetches the current Vedic time from `http://localhost:8000/vedic_time`.
